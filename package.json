{
  "name": "pi-kit",
  "private": true,
  "type": "module",
  "scripts": {
    "check:models": "node scripts/check-model-access.mjs",
    "check:models:live": "node scripts/check-model-access.mjs --live",
    "test": "node --test --experimental-strip-types 'extensions/**/*.test.ts'",
    "test:ci": "node --test --experimental-strip-types 'extensions/**/*.test.ts'"
  },
  "pi": {
    "prompts": [
      "prompts/*.md"
    ]
  },
  "devDependencies": {
    "fast-check": "^4.0.0"
  }
}
