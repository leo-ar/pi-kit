# Unsupported facts

- Session length alone is sufficient to judge bloat.
- The v1 heuristic is already enough without timeline validation.
- The archive ranking by itself proves that the warnings arrive at the right time.
- One temporary SQLite lock invalidates the validation setup.
- The archive analysis alone proves a 5x+ reduction in token-cached reads.
