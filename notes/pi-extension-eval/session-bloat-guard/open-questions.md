# Open questions

- Is the v1 tool-count heuristic enough to materially reduce repeated token-cached reads on real recorded sessions?
- Does session length materially improve interpretation of the score?
- Are false positives rare enough to keep the UI calm?
- Do any sessions need only a tiny extra signal to become clear?
- Is the single-circle UI sufficient during normal use?
- Can reload/resume consistently restore the same logical score from session data?
- At what event/turn points do healthy sessions stay green and unhealthy sessions move to yellow/orange/red?
