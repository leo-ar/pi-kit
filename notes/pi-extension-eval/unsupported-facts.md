# Unsupported facts

- `read-outline` is always the most valuable extension.
- `context-pruner` is consistently beneficial across sessions.
- `smart-compact` can be proven better than the default compaction path from the archive alone.
- `pi-hashline-edit-pro` is a clear overall win in every workflow.
- Hashline editing would improve token efficiency in the same way `read-outline` does.
